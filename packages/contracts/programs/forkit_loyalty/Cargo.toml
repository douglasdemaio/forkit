[package]
name = "forkit-loyalty"
version = "0.1.0"
description = "ForkIt loyalty program (Phase 2 stub)"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "forkit_loyalty"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.30.0"
